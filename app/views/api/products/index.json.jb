@products.map do |product|
    {
    id: product.id,
    name: product.name,
    price: product.product_tax,
    image_url: product.image_url,
    description: product.description,
    discounted: product.is_discounted
    }
  end
